export class Create_Account {

        UserId: string;// Foreign Key: User tablosuna referans

        BankId: string; // banka idsi

        //Balance :number ; // Hesap bakiyesi

        Currency: string ;  // Para birimi (örneğin USD, EUR)
}
